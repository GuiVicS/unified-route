# ===========================================
# API Bridge - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e configure os valores
# NUNCA commite o arquivo .env no repositório!

# ==========================================
# BANCO DE DADOS
# ==========================================
DATABASE_USER=apibridge
DATABASE_PASSWORD=SuaSenhaSegura123!
DATABASE_NAME=apibridge
DATABASE_PORT=5432

# ==========================================
# SEGURANÇA (OBRIGATÓRIO ALTERAR!)
# ==========================================
# Gere novas chaves com:
# openssl rand -base64 32  (para MASTER_KEY e SESSION_SECRET)
# openssl rand -base64 64  (para JWT_SECRET)

MASTER_KEY=
JWT_SECRET=
SESSION_SECRET=

# ==========================================
# ADMINISTRADOR
# ==========================================
ADMIN_EMAIL=admin@suaempresa.com
ADMIN_PASSWORD=SuaSenhaAdmin123!

# ==========================================
# SERVIDOR
# ==========================================
PORT=3000
BASE_URL=https://api.suaempresa.com
RATE_LIMIT_PER_MIN=60
UPSTREAM_TIMEOUT_MS=15000

# Origens CORS permitidas (separadas por vírgula)
CORS_ORIGINS=https://app.suaempresa.com,https://admin.suaempresa.com
